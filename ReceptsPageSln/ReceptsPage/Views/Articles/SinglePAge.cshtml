@model ArticleP
@{
    ViewData["Title"] = "SinglePage";
}
<div class="row">
    <div class="col-md-12 ">

        <h1 class="text-center single-page-h1">@Model.Title</h1>
        <div class="col-lg-11 single-page-desc">
            <p class="single-page-p">@Model.Description</p>

            <div class="content-justify ">
                @{
                    var base64 = Convert.ToBase64String(Model.ImgGeneral);
                    var imgsrc= string.Format("data:image/jpeg;base64,{0}",base64);
                }
                <img class="single-page-img" src="@imgsrc" alt="Alternate Text11" />
            </div>

        </div>

    </div>
</div>